= form_for @product_comment do |f|
  - if @product_comment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product_comment.errors.count, "error")} prohibited this product_comment from being saved:"
      %ul
        - @product_comment.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit 'Save'
